import './index.css'


const TabItem = (props) => {
    const {tabsList} = props
    const {displayText,tabId} = tabsList
    return(
        <div>
            <h1>TabItem</h1>
        </div>
    )
}

export default TabItem